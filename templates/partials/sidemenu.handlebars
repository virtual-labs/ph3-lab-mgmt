<div class="nav flex-column sidebar flex-shrink-1 flex-md-shrink-0 pr-4" id="menu">
  {{#each lus}}
    <div>
      <div class="d-flex">
        <a href="{{this.target}}" class="py-2 flex-grow-1 {{#if isCurrentItem}} current-item {{/if}}">
          {{this.label}}
        </a>
        {{#if this.tasks}}
          <button class="btn btn-white" type="button"
            data-bs-toggle="collapse" data-bs-target="#t-{{this.id}}"
            aria-controls="{{this.id}}">
            <i class="fa fa-angle-down"></i>
          </button>
        {{/if }}
      </div>
      <div id="t-{{this.id}}"
        {{#if isCurrentLU }} class="show" {{/if}}
          {{#unless isCurrentLU }} class="collapse" {{/unless}}
        >
        <div class="px-1 d-flex flex-column tasks">
          {{#each tasks}}
            <a href="{{this.target}}" class="py-1 {{#if isCurrentItem}} current-item {{/if}}"> {{this.label}} </a>
          {{/each}}
        </div>
      </div>
    </div>
  {{/each}}
</div>